package com.example.recipes_app.utils

import com.example.recipes_app.domain.models.FilterSettings

fun filterSettingsToMap(filterSettings: FilterSettings): Map<String, String> {
    val result = mutableMapOf<String, String>()
    return result
}